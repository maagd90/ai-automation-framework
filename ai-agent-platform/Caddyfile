# Example Caddy config for TLS termination in front of agent-ui + agent-api.
# Replace your-domain.com with your public hostname.

your-domain.com {
  encode gzip

  handle /api/* {
    reverse_proxy agent-api:3001
  }

  handle {
    reverse_proxy agent-ui:80
  }
}
